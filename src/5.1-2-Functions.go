package main

/*
import (
	"fmt"
)

func main() {
	hello()
	hello2("Juan", "Perez")

	//cadena := "Valor Actual"
	// Paso de parámetro por "Valor" NO se modifica
	var cadena string = "Valor Actual"
	cambiar_valor(cadena)
	fmt.Printf("Revisando el valor de -cadena- por Referencia %s \n", cadena)

	// Usando funciones con parametro de referencia.
	var cadena2 string = "Cambiar Valor"
	cambiar_valor2(&cadena2) // Pasando la dirección de Memoria.
	fmt.Printf("Cambiando el contenido -cadena2-= '%s' por referencia, punteros \n", cadena2)

} // func main()

// Sin parámetros
func hello() {
	fmt.Printf("Hola Mundo \n")
}

// Con Parámetros
func hello2(firstName string, lastName string) {
	fmt.Printf("Hello %s , %s \n", firstName, lastName)
}

// Cambiando un valor, através del parámetro de una funcion
// Se le llama "Paso Por Valor", no se modifica, ya que es una copia
func cambiar_valor(valor string) {
	valor = "Nuevo Valor De Cadena"
}

// Cambiando un valor, através del parámetro de una funcion
// Se le llama "Paso Por Referencia", se modifica, se utilizan punteros
func cambiar_valor2(valor *string) {
	*valor = "Nuevo Valor De Cadena"

}
*/
