package main

/*
import (
	"fmt"
)

// Range = Permite iterar sobre, Map, Slide, Arreglos, y cadenas
func main() {
	nums := []uint8{2, 4, 6, 8}
	// Desplegara esta palabra en base a los numeros de elementos del "Slide"
	for range nums {
		fmt.Printf("EDTeam \n")
	}

	// Para imprimir el valor de cada elemento del "Slide"
	for indice, valor := range nums {
		fmt.Printf("Indice =  %d , Valor = %d \n", indice, valor)
	}

	// Alterando los valores del "slide"
	for i := range nums {
		nums[i] *= 2
	}
	fmt.Printf("Imprimir los numeros %d \n", nums)

}
*/
