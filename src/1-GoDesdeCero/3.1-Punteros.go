package main

/*
import (
	"fmt"
)

func main() {
	// Punteros = Variables que almacenann la direccion de memoria de un valor.
	var fruit string = "Manzana"
	fmt.Printf("El tipo : %T de la variable 'fruit' = %s \n", fruit, fruit)
	fmt.Printf("\n")
	//&fruit = Direccion de memoria.
	fmt.Printf("El tipo : %T de la variable 'fruit' = %s, Direccion de memoria = %v \n", fruit, fruit, &fruit)
	fmt.Printf("\n")

	// Para crear un puntero.

	var p *string
	// Apunta a la dirección de memoria.
	p = &fruit
	fmt.Printf("El tipo de dato = %T la direccion de memoria = %v de la variable 'fruit' \n ", p, p)

	// Obtener el valor(desreferenciacion) de la dirección de  memoria de la variable "fruit"
	fmt.Printf("\n")
	fmt.Printf("El tipo de dato = %T la direccion de memoria = %v de la variable 'fruit', obtener el valor de la direccion(Desreferenciacion)  = %s \n ", p, p, *p)

	// Ahora modificacion desde el puntero el valor de la variable.
	*p = "Perasssssssssss"
	fmt.Printf("\n")
	fmt.Printf("Asignado un nuevo valor utilizando los punteros \n")
	fmt.Printf("El tipo de dato = %T la direccion de memoria = %v de la variable 'fruit', obtener el valor de la dirección = %s \n ", p, p, *p)

}
*/
