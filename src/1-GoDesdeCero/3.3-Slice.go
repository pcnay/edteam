package main

/*
import (
	"fmt"
)

func main() {
	// Slice = Apuntadores a los Arrays. Por lo que no almacen datos.
	arreglo := [7]string{"Arriba", "Abajo", "Izquierda", "Derecha", "Verde", "Rojo", "Violeta"}

	// arreglo[:]
	fmt.Printf("El contenido del Arreglo %v \n", arreglo)
	var slice []string
	// Solo obtener un rango del "arreglo"
	// Importante el último valor "4" no se incluye, si se quisiera debe escribir "5"
	slice = arreglo[0:5]
	fmt.Printf("\n")
	fmt.Printf("El valor del slice = %v \n", slice)

	// Otro rango, solo los colores.
	var slice_color []string

	// 2da Rango ; solo los colores.
	// Se agrega el 7 ya que es excluyente, para que tome la última.
	slice_color = arreglo[4:7]
	fmt.Printf("\n")
	fmt.Printf("El valor del slice 2do rango (Colores) = %v \n ", slice_color)

	// Comprobando que los "slice" son apuntadores de los elementos del Array.
	// Tener encuenta que se crea un nuevo arreglo [4:7], [0,1,2,3]
	slice_color = arreglo[4:7]
	slice_color[0] = "Gris"
	// Cambiando el subindice 0 a Gris.
	fmt.Printf("\n")
	fmt.Printf("Cambiando un elemento en el subarreglo \n")
	fmt.Printf("El valor del slice 2do rango (Colores) = %v \n ", slice_color)

	fmt.Printf("\n")
	fmt.Printf("El nuevo valor en el 'arreglo'  %v \n ", arreglo)

	// Para tomar el primer elemento de un arreglo sin especificar.
	// slice_color = arreglo[:4]
	// slice color = arreglo[2:]
	// Todos los elementos del arreglo.
	// todos := arreglo[:]

}
*/
