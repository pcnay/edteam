package main

/*
import (
	"fmt"
)

// Parametros fijos
func sum(num1, num2 int) int {
	return num1 + num2
}

// Permite recibir numero variables de parametros, se modifica en la sección de parámetros
func sum_varias(nums ...int) int {
	var total int = 0
	for _, valor := range nums {
		total += valor
	}
	return total
}

func main() {
	var total_suma, total_suma2 int
	sum(1, 2)

	total_suma = sum_varias(1, 2, 3, 4, 5, 6)
	fmt.Printf("La suma total de la funcion -variatica- %d \n", total_suma)

	total_suma2 = sum_varias(1, 2)
	fmt.Printf("La suma total2 de la funcion -variatica- %d \n", total_suma2)

}
*/
