package main

/*
// Como ya se creo el modulo de "greet", se puede agregar a cualquier programa.
import (
	"fmt"
	"github.com/pcnay/edteam.git/greet"
	//"rsc.io/quote"
	//quoteV3 "rsc.io/quote/v3"
	"rsc.io/quote/v3"
)

func main() {
	fmt.Printf("Imprimiendo saludo en Ingles = %s \n ", greet.English())
	fmt.Printf("Imprimiendo saludo desde la libreria -quote- = %s \n", quote.HelloV3())
	fmt.Printf("Usando la version 3, con concurrencia  %v \n", quote.Concurrency())

}

/*
// Como ya se creo el modulo de "greet", se puede agregar a cualquier programa.
Esta version es para cuando se actualiza a una vez mayor.
import (
	"fmt"
	"github.com/pcnay/edteam.git/greet"
	"rsc.io/quote"
	quoteV3 "rsc.io/quote/v3"
)

func main() {
	fmt.Printf("Imprimiendo saludo en Ingles = %s \n ", greet.English())
	fmt.Printf("Imprimiendo saludo desde la libreria -quote- = %s \n", quote.Hello())
	fmt.Printf("Usando la version 3, con concurrencia \n", quoteV3.Concurrency())

}

*/
