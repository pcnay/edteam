package main

/*
import (
	"fmt"
)

func main() {
	type cursos struct {
		Name     string
		Profesor string
		Country  string
	}

	// Instanciar la clase, Objeto
	db := cursos{
		Name:     "Base",
		Profesor: "Juan Mendez",
		Country:  "Mexico",
	}

	fmt.Printf("Imprimir el contenido de los campos de -struct- %+v \n", db)

	// Otra forma de crearlo, con solo indicando el contenido de los atributos de la clase.
	git := cursos{"Git", "Beto", "Bolivia"}
	fmt.Printf("Imprimir el contenido de otra clase(struct) %+v \n", git)

	// Asingando un valor
	css := cursos{Profesor: "Alvaro"}
	fmt.Printf("Imprimir el objeto -css- donde solo se asigna el nombre del Profesor %+v \n", css)

	// Accesando a uno de los atributos de la clase
	fmt.Printf("Clase(Struct) db : %+v \n ", db.Name)
	fmt.Printf("Clase(Struct) git : %+v \n ", git.Country)
	fmt.Printf("Clase(Struct) css : %+v \n ", css.Profesor)

	// Punteros
	p := &db
	fmt.Printf("Mostrando el contenido de la clase -db- %+v \n", db)
	fmt.Printf("Mostrando la variable puntero de la clase -db- %+v \n", p)

	// Para asignar un valor a un atributo.
	// Se tiene que desreferenciar el puntero.
	(*p).Country = "Argentina"

	// Otra forma: Go lo desreferencia y asigna el valor es las legible
	p.Country = "Brasil"
	fmt.Printf("Imprimiendo el valor asignado al atributor de la clase -db \n")
	fmt.Printf("Mostrando el contenido de la clase -db- %+v \n", db)
	fmt.Printf("Mostrando la variable puntero de la clase -db- %+v \n", p)

}
*/
