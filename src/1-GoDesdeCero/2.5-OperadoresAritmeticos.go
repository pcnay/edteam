package main

/*
func main() {
	// Operadores Aritmeticos: En ese orden de ejecución (), *, /, %, +, -
	// En la ejecución de los parentesis, se sigue el mismo orden de adentro hacia afuera.

	// Operadores de asignancion : =, +=, -=, *=, /=, %=
	// += Se suma y se asigna, y aplica para los demas.

	// Declaración de post-incremento = ++, post-decremento = --
	// (No es una expresión sino una declaración.)
	// expresion =  4 + 5*2
	// var c int = 0
	// declaracion = c++ (c = c+1; asigna y suma 1)

}
*/
