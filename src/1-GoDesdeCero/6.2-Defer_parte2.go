package main

/*
import (
	"fmt"
	"os"
)

func main() {
	// Se utilizan para limpiar recursos, cerrar archivos, cerrar conexiones de base de datos, conexiones de red.
	// Esta sintaxis es cuando se ejecuta el script en la misma carpeta donde esta el código fuente.
	// ObjetoArchivo, err := os.Create("hello5.txt")

	ObjetoArchivo, err := os.Create("src/hello6.txt")
	if err != nil {
		fmt.Printf("Ocurrio un error al crear el archivo: %v", err)
		return
	}
	// Se cerrara, independientemente si se crea o no el archivo, una vez terminada la ejecucion de la funcion en donde esta contenido, en este caso en el "main"
	defer ObjetoArchivo.Close()

	_, err = ObjetoArchivo.Write([]byte("Hello EDteam"))
	if err != nil {
		fmt.Printf("Ocurrio un error al escribir en el archivo: %v", err)
		return
	}

}
*/
