package main

/*
import (
	"fmt"
)

func main() {
	// Definiendo el "map"
	//map[TipoDato-Llave]TipoDato-Valor
	animals := make(map[string]string)
	animals["cat"] = "Cats"
	animals["dog"] = "Perros"

	fmt.Printf("Imprimiendo el contenido del MAP %v \n", animals)

	// Otra forma de declararlo.
	fruits := map[string]string{
		"apple":  "Manzana",
		"lime":   "Lima",
		"banana": "Platano",
	}
	fmt.Printf("Imprimiendpo el contenido de -fruit- %v \n ", fruits)

	fmt.Printf("Borrando un elemento del MAP \n")
	delete(fruits, "lime")

	fmt.Printf("Imprimiendo de nuevo el MAP, con elemento borrado %v \n", fruits)

	fmt.Printf("Obtener un elemento del MAP \n")
	fmt.Printf("El valor del indice -banana- %v \n ", fruits["banana"])

	// Cuando se quiera recuperar un valor que no exite, retornara "Value Zero" dependiendo del tipo de datos que se este manejando.
	// String = ""; int = 0 ; ,,,,

	// Cuando retorna un valor, lo hara con dos parámetros.
	// contenido,existeLlave := fruits["pine"]
	// Desplegara una cadena en blanco, y False.
	contenido, existe := fruits["pine"]
	fmt.Printf("Imprimiendo los valores que retorna un contenido de MAP que no existe %v, %v \n", contenido, existe)

	// Si no existe el indice lo creara.
	// Como no se utiliza la variable "contenido", se coloca "_"
	if _, existe := animals["gorilla"]; !existe {
		animals["gorilla"] = "Gorila"
	}

	fmt.Printf("Imprimiendo el Map -animals- con el nuevo elemento %v \n", animals)

}
*/
