package main

/*
import (
	"fmt"
)

func main() {
	// Recorriendo cadenas.
	var cadena string
	cadena = "Cadena de texto"

	// Imprimiendo solo contenido de la cadena.
	for _, valor := range cadena {
		// Imprime el byte que le corresponde a cadena
		fmt.Printf("Valor = %v \n", valor)
		// Para que imprima los caracteres de la cadena, se tiene que hacerlo Casting (string)
		fmt.Printf("Valor = %s \n", string(valor))
	}
}
*/
